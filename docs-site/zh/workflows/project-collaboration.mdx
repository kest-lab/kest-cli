---
title: 项目协作
description: Kest 中的项目角色、成员关系与工作区边界。
---

# 项目协作

Kest 当前存在两层协作模型：

- `Projects`，角色为 `owner`、`admin`、`write`、`read`
- `Workspaces`，角色为 `owner`、`admin`、`editor`、`viewer`

项目成员关系主要控制 API 资产与执行权限，工作区则适合更宽泛的协作容器。
